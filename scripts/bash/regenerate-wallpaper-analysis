#!/bin/bash
# Force-regenerate wallpaper analysis by clearing trigger files and re-running cache builder

. "$(dirname "$0")/lib.sh"

CACHE_VERSION_FILE="$SKWD_CACHE/wallpaper/cache-version.txt"
SCRIPT="$SKWD_INSTALL/scripts/bash/check-wallpaper-cache"
LOG="$SKWD_CACHE/wallpaper-cache-regen.log"

echo "🔄 Triggering wallpaper cache regeneration..."

# Clear version and last-run markers to force re-analysis
rm -f "$CACHE_VERSION_FILE"
rm -f "$SKWD_CACHE/wallpaper/analysis-lastrun"
rm -f "$SKWD_CACHE/wallpaper/colors-lastrun"
rm -f "$SKWD_CACHE/wallpaper/tags-lastrun"

# Launch cache builder in background with nohup
nohup bash -c "$SCRIPT" > "$LOG" 2>&1 &
disown

echo "✓ Regeneration started (pid $!, log: $LOG)"
echo ""
echo "Monitor progress:"
echo "  tail -f $LOG"
echo "  tail -f $SKWD_CACHE/analyze-wallpapers.log"
echo "  watch -n 2 $SKWD_INSTALL/scripts/bash/ollama-status"
